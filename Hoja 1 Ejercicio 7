#include <iostream>
#include <conio.h>

using namespace std;

int main()
{
	int N1, N2;
	cout << "Ingrese dos números enteros (N1 N2): " << endl;
	cin >> N1; cin >> N2;
	if (N1 % N2 == 0)
	{
		cout << N1 << " es multiplo de " << N2 << endl;
	}
	else
	{
		cout << N1 << " NO es multiplo de " << N2 << endl;
	}
		_getch();
}
